# Legacy Arrival Atlas homepage

This directory preserves the previous `ArrivalAtlasHomepage` implementation and its content maps.

- It is not part of the active Next.js route tree.
- The active homepage is `components/social-world/SocialWorldHomepage.tsx`.
- Reusable partnership copy has moved into the active Contact and About surfaces.
- The enterprise image library remains under `public/images/enterprise/` so the archived direction can still be inspected without broken assets.

Do not edit this directory when changing the live homepage unless the legacy direction is explicitly restored.
